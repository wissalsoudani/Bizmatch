import { BesoinOffreFilterPipePipe } from "./besoinoffre-filter-pipe.pipe";

describe('BesoinOffreFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new BesoinOffreFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
